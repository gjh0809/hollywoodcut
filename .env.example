# .env.example
# ==========================================
# 安全架构说明：
# 本项目为纯静态页面架构，前端代码会直接在浏览器端运行。
# 按照你的要求："确保任何个人信息没有泄漏风险，避免写在前端代码里"。
# 本项目不将真实环境参数（如 API_KEY）打包进代码，也不需要配置构建时变量注入。
# API Key 在使用时由用户于前端页面手动填写，并通过 LocalStorage 离线保存在浏览器端。
# 该文件仅作说明用途。

GEMINI_API_KEY=your_gemini_api_key_here
